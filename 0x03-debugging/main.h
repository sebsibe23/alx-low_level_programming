#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
void positive_or_negative(int w);
int largest_number(int q, int w, int r);
void print_remaining_days(int mm_, int dd_, int yy_);
int convert_day(int mm_, int dd_);
#endif
