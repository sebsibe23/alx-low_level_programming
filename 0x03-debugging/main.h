#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
void positive_or_negative(int d);
int largest_number(int q, int w, int e);
void print_remaining_days(intmm_, int dd_, int yy_);
int convert_day(int mm_, int dd_);
#endif
