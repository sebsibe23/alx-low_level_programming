#include "main.h"

/**
 * add - sums two integers and returns the value
 * @g: int to be added to f
 * @f: int to be added to f
 * Return: sum of g and f
 */
int add(int f, int g)
{
	int d;

	d = f + g;

	return (d);
}
