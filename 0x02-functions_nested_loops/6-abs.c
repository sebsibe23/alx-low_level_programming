#include "main.h"

/**
 * _abs - computes an integer's absolute value.
 * @t: the integer to be checked
 * Return: integer's absolute value
 */
int _abs(int t)
{
        if (t >= 0)
        {
                return (t);
        }
        return (-t);
}
