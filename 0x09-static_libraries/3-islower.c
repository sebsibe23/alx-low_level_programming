#include "main.h"

/**
 * _islower - analyzes lowercase characters
 * @ch_ar: the character to check
 * Return: 1 if ch_ar is lowercase, 0 otherwise
 */
int _islower(int ch_ar)
{
	return (ch_ar >= 'a' && ch_ar <= 'z');
}
