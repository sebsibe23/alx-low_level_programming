#include <stdio.h>

/**
 * main- Prints digits 0 through 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int k;

	for (k = 48; k < 58; k++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
