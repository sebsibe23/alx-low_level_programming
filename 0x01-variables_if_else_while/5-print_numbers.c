
#include <stdio.h>

/**
 * main- Prints digits 0 through 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
	{
		printf("%d", p);
	}
	putchar('\n');
	return (0);
}
